<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp">

        <!-- Logo and Illustration -->
        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_marginTop="40dp"
            android:layout_marginBottom="24dp"
            android:contentDescription="@string/app_name"
            android:src="@drawable/ic_lock"
            app:tint="?attr/colorPrimary" />

        <!-- Email Form Layout -->
        <LinearLayout
            android:id="@+id/email_form_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="visible">

            <!-- Title Text -->
            <TextView
                style="@style/WanderPlan.Text.Headline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="Forgot Password?"
                android:textColor="@color/navy" />

            <TextView
                style="@style/WanderPlan.Text.Body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="40dp"
                android:gravity="center"
                android:text="Enter your email address and we'll send you instructions to reset your password"
                android:textColor="@color/medium_grey" />

            <!-- Email Input -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/email_input_layout"
                style="@style/WanderPlan.TextInputLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="32dp"
                android:hint="Email Address"
                app:startIconDrawable="@drawable/ic_email"
                app:startIconTint="?attr/colorPrimary">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/email_edit_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:inputType="textEmailAddress"
                    android:textSize="16sp" />

            </com.google.android.material.textfield.TextInputLayout>

            <!-- Send Email Button -->
            <Button
                android:id="@+id/send_email_button"
                style="@style/WanderPlan.Button.Primary"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="24dp"
                android:text="Send Verification Email"
                android:textSize="16sp" />

        </LinearLayout>

        <!-- Confirmation Layout -->
        <LinearLayout
            android:id="@+id/confirmation_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

                         <!-- Success Icon -->
             <ImageView
                 android:layout_width="80dp"
                 android:layout_height="80dp"
                 android:layout_marginBottom="24dp"
                 android:contentDescription="Email sent"
                 android:src="@drawable/ic_email"
                 app:tint="@color/mint_green" />

            <!-- Confirmation Text -->
            <TextView
                style="@style/WanderPlan.Text.Headline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="Check Your Inbox"
                android:textColor="@color/navy" />

            <TextView
                style="@style/WanderPlan.Text.Body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:gravity="center"
                android:text="We've sent password reset instructions to:"
                android:textColor="@color/medium_grey" />

            <TextView
                android:id="@+id/confirmation_email"
                style="@style/WanderPlan.Text.Body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="32dp"
                android:gravity="center"
                android:text="user@example.com"
                android:textColor="?attr/colorPrimary"
                android:textStyle="bold" />

            <!-- Resend Email Button -->
            <Button
                android:id="@+id/resend_email_button"
                style="@style/WanderPlan.Button.Secondary"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:text="Resend Email"
                android:textSize="16sp" />

            <!-- Help Button -->
            <Button
                android:id="@+id/help_button"
                style="@style/WanderPlan.Button.Outline"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginBottom="24dp"
                android:text="Not Receiving Email? Get Help"
                android:textSize="14sp" />

            <!-- Instructions -->
            <TextView
                style="@style/WanderPlan.Text.Body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="24dp"
                android:gravity="center"
                android:text="Check your spam folder and wait 5-10 minutes for delivery."
                android:textColor="@color/medium_grey"
                android:textSize="14sp" />

        </LinearLayout>

        <!-- Back to Login Link -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:layout_marginBottom="16dp">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="Back arrow"
                android:src="@drawable/ic_arrow_back"
                app:tint="?attr/colorPrimary" />

            <TextView
                android:id="@+id/back_to_login_text"
                style="@style/WanderPlan.Text.Body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:padding="8dp"
                android:text="Back to Login"
                android:textColor="?attr/colorPrimary"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Debug Button -->
        <Button
            android:id="@+id/debug_button"
            style="@style/WanderPlan.Button.Outline"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="🔧 Debug Password Reset"
            android:textSize="12sp" />

    </LinearLayout>

</ScrollView>


